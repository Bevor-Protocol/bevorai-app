import { AdminTeamsClient } from "@/components/admin/admin-teams-client";
import { AsyncComponent } from "@/types";

const AdminTeamsPage: AsyncComponent = async () => {
  return <AdminTeamsClient />;
};

export default AdminTeamsPage;
